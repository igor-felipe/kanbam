import * as DB from "../db/column_repository";

export const { create } = DB;

export const { update } = DB;

export const { getOne } = DB;

export const { findMany } = DB;

export const { deleteOne } = DB;
